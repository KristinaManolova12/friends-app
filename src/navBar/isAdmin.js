import React from 'react'
import IsAdminFunc from './isAdminComponents'

function IsAdmin(props) {
    //  const isAdmin = props.admin
   
    
    // if (isAdmin) {
        return <IsAdminFunc />;
  // }
}

export default IsAdmin